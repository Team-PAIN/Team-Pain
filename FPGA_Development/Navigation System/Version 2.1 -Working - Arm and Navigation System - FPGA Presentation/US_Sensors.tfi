


// Instantiate the module
US_Sensors instance_name (
    .CLK(CLK), 
    .SIG1(SIG1), 
    .SIG2(SIG2), 
    .SIG3(SIG3), 
    .SIG4(SIG4), 
    .SCLK_50MHz(SCLK_50MHz), 
    .DEBOUCED_SCLK(DEBOUCED_SCLK), 
    .ANGLE(ANGLE), 
    .ANGLE_DIRECTION(ANGLE_DIRECTION), 
    .DISTANCE1_DEBOUNCED(DISTANCE1_DEBOUNCED), 
    .DISTANCE2_DEBOUNCED(DISTANCE2_DEBOUNCED), 
    .DISTANCE3_DEBOUNCED(DISTANCE3_DEBOUNCED), 
    .DISTANCE3_DEBOUNCED(DISTANCE3_DEBOUNCED)
    );


